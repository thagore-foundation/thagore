// stdlib entrypoint
//
// Import submodules so `use std as std` provides common helpers.
use prelude as prelude
use option as option
use result as result
use alloc as alloc
use buffer as buffer
use bytes as bytes
use char as char
use collections as collections
use dylib as dylib
use platform as platform
use gc as gc
use sys_platform as sys_platform
use env as env
use fs as fs
use http as http
use time as time
use json as json
use list as list
use map as map
use math as math
use mem as mem
use path as path
use ptr as ptr
use regex as regex
use set as set
use string as string
use sys as sys
use io as io
use process as process
use sys_linux as sys_linux
use sys_windows as sys_windows
use sys_bsd as sys_bsd
use sys_macos as sys_macos
use sys_mach as sys_mach
