[package]
name = "thagore-interpreter"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
thagore-ast = { path = "../ast" }

[dev-dependencies]
bumpalo = { version = "3", features = ["collections"] }
thagore-lexer = { path = "../lexer" }
thagore-parser = { path = "../parser" }
