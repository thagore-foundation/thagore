[package]
name = "thagore-ast"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
bumpalo = { version = "3.19.0", default-features = false, features = ["collections"] }
