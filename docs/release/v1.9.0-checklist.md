# v1.9.0 Release Checklist (Span System + Token Pipeline)

Release date: 2026-03-05

This release targets one milestone only: `v1.9`.

## Scope (from ROADMAP v1.9)

- [x] Add `Span` type and wire spans into frontend tokens.
- [x] Add `SourceMap` for offset -> line/column lookup.
- [x] Keep token stream in parser context (remove token discard path).
- [x] Propagate spans into AST/Core IR nodes used in current pipeline.
- [x] Print caret diagnostics in `thagc check`.

## Build Gate Verification

- [x] Compiler build succeeds:
  - `cmake --build build`
- [x] Valid source still checks cleanly:
  - `./build/compiler/thagc check examples/cli-tool/main.tg`
- [x] Parse error renders caret underline:
  - `./build/compiler/thagc check /tmp/v19_parse_error.tg`

## Release Gate

- [x] Tag prepared: `v1.9.0`
- [x] Changelog entry includes `span-aware diagnostics`.
- [ ] GitHub release artifacts publish (Linux + macOS) via `.github/workflows/release.yml` after pushing tag.
