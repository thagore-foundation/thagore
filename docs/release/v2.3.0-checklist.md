# v2.3.0 Release Checklist (HM Inference + Generics Baseline)

Release date: 2026-03-06

This release targets one milestone only: `v2.3`.

## Scope (from ROADMAP v2.3)

- [x] Add unification engine (`Unifier`) with `fresh/unify/apply`.
- [x] Add type-level HIR inference/check APIs (`infer_expression_ty`, `check_expression_ty`).
- [x] Integrate generic tree-type checking for `Option<T>` / `Result<T,E>` on `let` and `return`.
- [x] Add release-gate suites under `tests/inference/` and `tests/generics/`.
- [x] Keep no-regression behavior on existing parser/typecheck/import/e2e lanes.

## Build Gate Verification

- [x] Compiler build succeeds:
  - `cmake --build build`
- [x] v2.3 inference + generics suites pass:
  - `THAGC_BIN=build/compiler/thagc python3 -m unittest tests.inference.test_hm_inference tests.generics.test_monomorphized_generics`
- [x] Regression suites pass:
  - `THAGC_BIN=build/compiler/thagc python3 -m unittest tests.parity.test_syntax_contract_alignment tests.parity.test_v22_hir_pipeline tests.parity.test_v23_hm_generics_pipeline tests.integration.test_language_feature_completion tests.integration.test_import_system tests.e2e.test_build_and_run`

## Release Gate

- [x] Tag prepared: `v2.3.0`
- [x] Changelog entry added for v2.3.0
- [ ] GitHub release artifacts publish (Linux + macOS) via `.github/workflows/release.yml` after pushing tag.
