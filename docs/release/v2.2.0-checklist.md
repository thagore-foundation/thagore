# v2.2.0 Release Checklist (HIR + Bidirectional Type Checking)

Release date: 2026-03-06

This release targets one milestone only: `v2.2`.

## Scope (from ROADMAP v2.2)

- [x] Add HIR expression nodes.
- [x] Add HIR lowering pass from `AstExpr`.
- [x] Add type representation layer (`Ty*`).
- [x] Add bidirectional type checker API (`infer` + `check`) and integrate into frontend typechecking flow.
- [x] Keep no-regression behavior on existing language/stdlib/import gates.

## Build Gate Verification

- [x] Compiler build succeeds:
  - `cmake --build build`
- [x] Main integration/e2e regression suites pass:
  - `THAGC_BIN=build/compiler/thagc python3 -m unittest tests.integration.test_language_feature_completion tests.integration.test_import_system tests.e2e.test_build_and_run`
- [x] Parity suites for v2.2 wiring pass:
  - `python3 -m unittest tests.parity.test_syntax_contract_alignment tests.parity.test_v22_hir_pipeline`

## Release Gate

- [x] Tag prepared: `v2.2.0`
- [x] Changelog entry includes:
  - `typed HIR, bidirectional type checker, type-mismatch errors with source underlines.`
- [ ] GitHub release artifacts publish (Linux + macOS) via `.github/workflows/release.yml` after pushing tag.
