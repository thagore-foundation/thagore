# v0.6.0 Release Checklist (Concurrency Primitives Alpha)

This release intentionally targets one milestone only: `v0.6`.
No `v0.7+` scope is accepted in this cut.

## Scope (from ROADMAP v0.6)

- [x] Task scope foundation (`thag_task_scope_t`) production path (no raw thread-per-task model)
- [x] Nursery prototype (`thag_nursery_create`) wired to scope runtime
- [x] Parent-child cancellation propagation behavior
- [x] Async scheduler/event loop runtime API
- [x] `Rc<T>` / `Arc<T>` semantic handling path in compiler checks
- [x] Automatic `Send` / `Sync` checks
- [x] Diagnostic `E_SEND_SYNC_004` with actionable Arc hint
- [x] Child-task leak regression checks
- [x] Cancel/timeout deterministic stress checks

## Gate Verification

- [x] Build passes with LLVM lane (`build-llvm21-run`)
- [x] Full tests pass
- [x] Concurrency regression native suite passes
- [x] Contracts for structured concurrency and memory model exist

## Artifacts

- Version bumped to `0.6.0` in `VERSION`
- Release notes: `docs/release/v0.6.0-release-notes.md`
- Milestone state updated in `ROADMAP.md`

