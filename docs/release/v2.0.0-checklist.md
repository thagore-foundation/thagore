# v2.0.0 Release Checklist (Real Recursive-Descent Parser)

Release date: 2026-03-06

This release targets one milestone only: `v2.0`.

## Scope (from ROADMAP v2.0)

- [x] Parser context consumes real token stream.
- [x] Recursive-descent expression/statement parsing path is active.
- [x] Top-level parser handles item declarations on token stream.
- [x] Legacy line-by-line parser path removed from primary frontend entry.
- [x] Backend no longer depends on third-pass frontend string re-parse for primary expression lowering.

## Build Gate Verification

- [x] Compiler build succeeds:
  - `cmake --build build`
- [x] Regression suites pass with current compiler binary:
  - `THAGC_BIN=build/compiler/thagc python3 -m unittest tests.integration.test_language_feature_completion tests.integration.test_import_system tests.e2e.test_build_and_run`

## Release Gate

- [x] Tag prepared: `v2.0.0`
- [x] Changelog entry includes:
  - `replaced line-by-line parser with recursive-descent token-stream parser; 3-pass re-parse eliminated.`
- [ ] GitHub release artifacts publish (Linux + macOS) via `.github/workflows/release.yml` after pushing tag.
