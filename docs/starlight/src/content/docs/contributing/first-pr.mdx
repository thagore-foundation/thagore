---
title: Contributing — First PR
description: How to contribute to Thagore — from setting up your development environment to submitting your first pull request.
---

import { Aside, Steps } from "@astrojs/starlight/components";

# Contributing — Your First PR

## Getting Started

### 1. Fork and Clone

```bash
git clone https://github.com/YOUR_USERNAME/thagore.git
cd thagore
```

### 2. Set Up Prerequisites

Ensure you have:
- **LLVM 21.x** — installed and on PATH
- **Current hosted toolchain** — downloaded from the latest release
- **Python 3.x** — for build scripts (optional)

See [Prerequisites](/install/prerequisites) for detailed installation instructions.

### 3. Install and verify the current toolchain

```bash
# Windows
cmd /c scripts\bootstrap.bat

# Linux/macOS
bash scripts/bootstrap.sh
```

### 4. Verify Your Setup

```bash
thagc build tests/fixtures/hello.tg -o hello.exe
.\hello.exe
# Should exit successfully
```

## Making Changes

### Source Code Structure

| Directory | Contains |
|-----------|----------|
| `crates/` | Core compiler crates |
| `tools/` | CLI, formatter, and related tooling |
| `stdlib/` | Shipped standard library modules |
| `examples/` | Example programs |
| `tests/` | Test suite |
| `tooling/` | Release and automation scripts |
| `docs/` | Documentation |

### Development Workflow

1. **Create a feature branch:**
   ```bash
   git checkout -b feature/my-change
   ```

2. **Make your changes** in the relevant Rust/std/runtime/docs source files

3. **Run targeted checks** for the area you changed

4. **Run tests:**
   ```bash
   cargo test --workspace
   ```

5. **Build sample programs** to verify nothing is broken:
   ```bash
   thagc build tests/fixtures/hello.tg -o hello.exe
   ```

## Code Style Guidelines

1. **Indentation**: Use 2 spaces (no tabs, no 4 spaces)
2. **Line endings**: Unix-style LF (not CRLF)
3. **File encoding**: UTF-8
4. **Naming conventions**:
   - Functions: `snake_case` — `parse_enum_name`, `validate_source`
   - Structs: `PascalCase` — `ProgramAstNative`, `CoreProgram`
   - Constants: `UPPER_SNAKE_CASE` (when applicable)
   - Private/internal functions: prefix with `_` — `_trim`, `_next_line_pos`
5. **Comments**: Use `#` for comments
6. **Function signatures**: Always include explicit return type

### Example

```thagore
# Calculate the dot product of two 2D vectors
func dot_product(ax: i32, ay: i32, bx: i32, by: i32) -> i32:
  return ax * bx + ay * by

func _validate_input(n: i32) -> i32:
  if (n < 0):
    return 1
  return 0
```

## Submitting a PR

### PR Checklist

Before submitting:
- [ ] Code compiles successfully
- [ ] Self-host test passes (`stage2 → stage2b`)
- [ ] Existing tests still pass
- [ ] New tests added for new functionality
- [ ] No C++ fallback introduced
- [ ] Code follows style guidelines

### CI Requirements

Your PR must pass all three CI pipelines with **3 consecutive green runs**:
1. ✅ **CI** — build and test
2. ✅ **Hosted Toolchain Matrix** — compiler and tooling checks on supported platforms
3. ✅ **Release (dry-run)** — release artifact generation

<Aside type="caution" title="Bootstrap Policy">
**Never** introduce a hidden legacy fallback or undocumented bootstrap path. Keep the hosted toolchain and any future bootstrap chain explicit and auditable.
</Aside>

## Where to Contribute

### Good First Issues
- Add new examples to `examples/`
- Improve documentation in `docs/starlight/`
- Add test cases to `tests/`
- Improve error messages in the compiler

### Intermediate
- Add new standard library functions to `lib/`
- Improve the autofix engine rules
- Enhance the CLI with new commands

### Advanced
- Work on the lowering pipeline
- Implement new language features (tracked by feature counters)
- Optimize the LLVM IR emission
- Port to new platforms
