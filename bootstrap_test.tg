import "src/compiler/lexer.tg" as lexer

func main() -> i32:
    print("Bootstrapping Lexer Test...")
    let code = "a = b + c"
    let total = lexer.tokenize(code)

    print("Tokenization completed.")

    print("Total:")
    print(total)
    return 0
