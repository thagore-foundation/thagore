import "lib/fs.tg" as fs
import "src/v0/lexer/lexer.tg"

func main() -> i32:
    let src = fs.read_text("examples/v0/hello_fn.tg")
    let result = v0_lex(src)
    if (result.error_count > 0):
        return 1
    return 0
