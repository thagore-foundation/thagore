# fixture: bench/fibonacci
# expect: ok
# description: computes fib(35) recursively for benchmark coverage
func fib(n: i32) -> i32:
  if (n < 2):
    return n
  else:
    return fib(n - 1) + fib(n - 2)

func main() -> i32:
  return fib(35) % 251
