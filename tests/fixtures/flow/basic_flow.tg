# fixture: flow/basic_flow
# expect: ok
# description: defines a flow with two stages and a compensation block
flow Transaction:
  stage acquire:
    let auth_state: i32 = 1
  stage commit:
    let commit_state: i32 = 2
  compensate:
    let rollback_state: i32 = 3

func main() -> i32:
  return 0
