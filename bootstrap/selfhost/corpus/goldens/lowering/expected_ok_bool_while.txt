fn=main;sig=()->bool;locals=[ready:bool];blocks=[entry,loop,exit];cfg=[entry->loop,loop->exit];ir=[entry:alloc(ready:bool),entry:const(bool),entry:store(ready:bool<-const:bool),entry:load(ready:bool),entry:loop(load:ready:bool),loop:const(bool),loop:assign(ready:bool<-const:bool),exit:load(ready:bool),exit:ret(load:ready:bool)]

