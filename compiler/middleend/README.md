# Middleend

Use this module when you need to change:

- typed/core IR model transformations
- lowering contracts between semantic model and backend

Primary code lives under:

- `compiler/include/thagc/middleend/`
- `compiler/src/middleend/`

