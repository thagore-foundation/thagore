# Backend

Use this module when you need to change:

- LLVM IR/object emission
- target triple and target-machine integration
- backend diagnostics and object-output behavior

Primary code lives under:

- `compiler/include/thagc/backend/`
- `compiler/src/backend/`

