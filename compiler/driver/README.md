# Driver

Use this module when you need to change:

- CLI command parsing and help/version behavior
- build command orchestration and pipeline entrypoints

Primary code lives under:

- `compiler/include/thagc/driver/`
- `compiler/src/driver/`

