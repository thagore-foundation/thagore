# module: stdlib/path
# description: Path helpers layered on top of std.fs.

import std.fs as fs

func getcwd() -> str:
  return fs.getcwd()

func join(left: str, right: str) -> str:
  return fs.path_join(left, right)

func is_dir(path: str) -> bool:
  return fs.is_dir(path)
