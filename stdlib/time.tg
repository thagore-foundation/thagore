# module: stdlib/time
# description: Minimal time helpers backed by the bundled Thagore runtime.

extern func thag_rt_now_ms() -> i64

# now_ms: Returns the current runtime clock in milliseconds.
func now_ms() -> i64:
  return thag_rt_now_ms()
